# {{PROJECT_NAME}} — Claude Project Guide

## Overview

<One paragraph describing what this project does and its primary tech stack.>

## Stack

| Layer | Technology |
|-------|-----------|
| Language | {{LANGUAGE}} |
| Framework | {{FRAMEWORK}} |
| Tests | {{TESTS}} |
| Container | {{CONTAINER}} |

## Dev Workflow

```bash
{{DEV_WORKFLOW}}
```

## Coding Standards

- Type hints required on all function signatures
- `X | None` not `Optional[X]`
- No docblocks — name things clearly instead
- Tests live alongside code, not in a separate `tests/` tree
- <Add your project-specific rules here>

## Key File Paths

| Purpose | Path |
|---------|------|
| Main entry point | `<path>` |
| Config | `<path>` |
| Tests | `<path>` |

## Development Pipeline (takshak)

```
/takshak:design <requirement | TICKET-ID>   → SDD + adversarial hardening
/takshak:breakdown <sdd-path>               → tasks (+ Jira); reply "approved" to unlock code
(implement)                                    → sdd-implementer runs the bug loop
/takshak:retro                              → capture learnings
/takshak:ship <env>                         → docs → tests → types → review → build → Jira
/takshak:dashboard                          → visual pipeline state (read-only)
/takshak:review <sdd | slug>                → guided walkthrough for a reviewer
/takshak:resume | :checkpoint | :remember   → continue, save/clear, write a memory
/takshak:doctor                             → installation health check
```

Commit messages reference the task (`T<id>`) or Jira key.

## Lessons Learned

<!-- /retro appends entries here automatically -->

## Do Not Do

- <Add project-specific anti-patterns here>
- Do not use `Optional[X]` — write `X | None`
- Do not add `# noqa` without writing a feedback memory explaining the suppression
